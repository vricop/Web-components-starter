{
  "name": "project-name",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "start": "live-server & npm run build:css & npm run build:css-min",
    "build:css": "sass --watch src/scss/main.scss:assets/css/main.css --style expanded",
    "build:css-min": "sass --watch src/scss/main.scss:assets/css/main.min.css --style compressed --no-source-map",
    "purge:css": "purgecss --config purgecss.config.js --out ./"
  },
  "author": "Víctor Rico Penalba",
  "license": "GPL-3.0-or-later"
}
